export enum Specials {
  PLUS_ONE = '+1',
  SWAP_HANDS = 'SWAP_HANDS',
  CHANGE_DIRECTION = 'CHANGE_DIRECTION',
  SKIP_NEXT_USER = '0',
  WILD_CARD = 'WILD_CARD',
  // placeholder to keep the card class singular and simple in that way
  NONE = 'NONE',
}
